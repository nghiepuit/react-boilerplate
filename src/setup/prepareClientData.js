/**
 * try catch to dispatch any action to load some data before start app. Ex: recent data, keyword for search
 * @param {*} store : redux-store { dispatch }
 */
export function prepareClientData(store) {}
